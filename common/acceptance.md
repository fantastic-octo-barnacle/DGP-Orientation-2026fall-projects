# 验收清单

## 项目范围

- `client-sync` 是必做层级。
- `server-async` 是最终必做层级。
- `server-sync` 是可选的中间层；可以先完成它再进入异步版本，也可以在确认自己能直接处理异步代码后跳过。
- 如果跳过同步服务端，请在成果说明中写明选择和实际完成范围；这不会降低异步服务端的要求。

Python 和 Rust 两条路线使用本清单，不因语言不同而改变任务要求。

## 起始代码

运行要完成的独立项目测试，再启动任一服务端执行 baseline 自查。确认 ping、注册、登录、令牌替换、退出和空文本列表可用。起始代码中的任务接口可能返回 501；完成某项任务后，应更新对应测试，不要继续把 501 当成最终要求。

## 第一层：客户端

- 客户端能完成全部接口操作，支持终端多行文本输入，并说明如何结束输入。
- 显示 HTTP 状态及结果，遇到 401 提示重新登录；网络失败后仍能继续操作或明确退出。
- echo 保留 Unicode、换行和空文本；delay 校验类型、范围并实际等待。
- 客户端从发起 HTTP 操作到读完响应总计最多 12 秒；单次读写超时不等于总期限。

使用完整参考服务端验证客户端，不要只用手工打印请求判断成功。

## 第二层：同步服务端（可选）

- 服务端能完成基础路由和账号流程，注册同名用户只能有一次成功。
- 上传、覆盖、读取、排序列表、删除行为符合协议，缺失文本返回 404。
- 两个用户保存同名文本互不影响，不能访问对方文本。
- 注销清理账号、文本和令牌，同名重新注册为空账号。
- 检查名称边界、UTF-8 字节上限、非法 JSON、字段类型及重复注册。
- `delay` 等待完成才返回结果；同步服务端按顺序处理请求。

可以使用自己的客户端或完整参考客户端验证同步服务端。

## 第三层：异步服务端

- 异步服务端完成同步服务端要求的业务行为。
- 异步端的 delay 不阻塞其他连接的 ping。
- 并发同名注册只成功一次；登录、文本修改和注销竞争时状态仍一致。
- 密码计算等阻塞工作不能占用事件循环，状态锁期间不能等待网络或 delay。
- 空闲、读取部分请求、执行 delay、发送响应时按 Ctrl-C，验证最多 2 秒结束在途处理并退出。

可以使用自己的客户端或完整参考客户端验证异步服务端。

## 服务端后段：令牌过期

- 用 `--token-ttl-seconds 2` 启动服务端：登录响应包含 `expires_in`，期限前可使用，到期返回 401。
- 期间发请求不会续期；重新登录后新令牌有效、旧令牌无效。
- 退出和注销同样撤销令牌，注销后同名重新注册为空账号。

## 边界和生命周期

- 上传、覆盖、读取、排序列表、删除行为符合协议，缺失文本返回 404。
- 验证客户端 12 秒总期限、请求读取 30 秒总期限；持续小块发送也不能延长期限。
- 单个请求失败不影响其他请求；没有遗留后台任务或无限等待。

[Python 阶段自查](../projects/python/self-check/README.md)和 [Rust 阶段自查](../projects/rust/self-check/README.md)覆盖常见场景，不代替完整验收。补充自己的测试，说明验证方法与已知限制。文档要求见[成果说明](deliverables.md)。
