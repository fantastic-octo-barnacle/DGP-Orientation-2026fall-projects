# HTTP 用户文本服务协议

使用 HTTP/1.1，默认地址 `http://127.0.0.1:7878`。请求体和成功响应为 UTF-8 JSON，携带 `Content-Type: application/json`。HTTP 库负责编码和分帧。

成功响应为 `{"data": ...}`。失败由 HTTP 状态码表示，响应体可带 `{"message": "原因"}`；说明文字不要求一致。请求不含自定义 id，错误不含自定义 code。

## 与任务层级的关系

协议定义最终行为；起始代码只实现其中一部分。

- 客户端层级使用完整参考服务端，先完成请求构造、输入处理、响应显示和客户端期限。
- 同步服务端层级（可跳过）实现业务路由和内存状态，不要求并发处理。
- 异步服务端层级实现同一套业务行为，并增加异步等待、并发状态一致性和有界退出。
- 令牌过期属于服务端任务的后段，不要求在刚开始运行基线时完成。

参考程序的完整客户端和异步服务端可从 [GitHub Releases](https://github.com/fantastic-octo-barnacle/DGP-Orientation-2026fall-projects/releases) 下载。参考程序用于交叉验证，不提供参考实现源码。

## 接口与任务

| 方法与路径 | 请求体 | 成功 data | 状态码 | 起始代码 |
|---|---|---|---|---|
| GET /ping | 无 | `"pong"` | 200 | 已实现 |
| POST /users | `{"username":"alice","password":"password1"}` | `{"username":"alice"}` | 201 | 已实现 |
| POST /sessions | 同注册 | `{"token":"…","expires_in":300}` | 200 | 已实现登录；`expires_in` 与过期机制后段添加 |
| DELETE /sessions/current | 无 | null | 200 | 已实现 |
| GET /texts | 无 | `["a","b"]` | 200 | 已实现 |
| POST /echo | `{"text":"你好\nRM"}` | 输入的文本字符串 | 200 | 待实现 |
| POST /delay | `{"milliseconds":1000}` | `{"milliseconds":1000}` | 200 | 待实现 |
| DELETE /users/me | 无 | null | 200 | 待实现 |
| PUT /texts/{name} | `{"text":"内容"}` | null | 200 | 待实现 |
| GET /texts/{name} | 无 | 文本字符串 | 200 | 待实现 |
| DELETE /texts/{name} | 无 | null | 200 | 待实现 |

待实现路由目前返回 501；完成后应返回表中的结果。请求 JSON 对象只包含列出的字段。带请求体的操作要求 JSON，即使 echo 的文本为空也要提交 `{"text":""}`。

## 用户与身份

用户名区分大小写，由 1–32 个 ASCII 字母、数字、下划线或连字符组成。密码为 8–128 个 Unicode 字符。重复注册返回 409，用户名或密码错误统一返回 401。

注册后需要登录。每个用户只有一个有效令牌；再次登录替换旧令牌。退出撤销当前令牌。注销删除用户、全部文本和令牌；之后可重新注册同名用户，旧令牌不能恢复有效。

退出、注销和所有文本接口需要 `Authorization: Bearer <token>`。其余接口公开。身份由服务端根据令牌确定，客户端不能指定文本所属用户。缺失、无效或过期令牌返回 401。

起始代码使用内存保存账号、令牌和文本，重启后清空；令牌暂不过期。服务端任务后段需要增加 `--token-ttl-seconds`，接受正整数，默认 300。从登录成功起计算固定有效期，不随操作续期；登录响应增加整数 `expires_in`。使用适合计算经过时间的时钟，达到期限即失效。

访问或修改受保护数据时检查有效性，身份检查和操作保持一致。已通过检查并完成的数据操作不因发送响应时到期而撤销。注销完成后，旧身份的操作不能重新写入文本。无需刷新令牌接口，过期记录的清理方式自行选择。

## 文本与输入边界

文本名称由 1–64 个 ASCII 字母、数字、下划线或连字符组成，区分大小写。每个用户可以保存多篇文本，同名上传覆盖。只访问自己的文本，列表按名称升序排列；同名文本可分别属于不同用户。获取、删除不存在的文本返回 404。

文本支持 Unicode、换行和空字符串，UTF-8 编码后最多 65,536 字节；echo 使用同样上限。超限返回 413。名称、字段类型或范围不合法返回 400。文本是合法 Unicode 字符串，不接受未配对的代理码位。

HTTP 请求体上限为 524,288 字节，包含 JSON 转义和字段名；超过返回 413。delay 的 milliseconds 为 0–10,000 的整数，布尔值和小数不属于整数。等待完成才返回结果。

未知路径返回 404，已知路径的不支持方法返回 405。错误响应不要求自定义错误码。框架处理的 HTTP 格式错误可使用其原生错误响应。

## 运行与并发

同步服务端按顺序处理业务请求。异步服务端并发处理：delay 不阻塞其他请求；密码计算等阻塞工作不能占用事件循环。注册同名用户只能有一次成功；并发文本操作、登录替换、注销与令牌检查须保持状态一致，不在持锁期间等待网络或 delay。

两个服务端分别运行，各自持有独立数据，不需要相互同步。每个程序使用一个进程中的存储，启动多个 worker 会产生多份状态，应保持单 worker。

最终程序要求：

- 客户端从发起 HTTP 操作到读完响应总计最多 12 秒；单次读写超时不等于总期限。
- 服务端从开始接收一个请求到完整读完请求最多 30 秒，分段发送不重置期限；超时关闭连接，可在能够响应时返回 408。空闲 keep-alive 可按框架默认策略关闭。
- Ctrl-C 后停止接收新请求，最多等待在途请求 2 秒，然后终止剩余处理并退出；空闲时也能及时退出。
- 参数错误有明确提示，运行错误不导致其他客户端的状态损坏。

HTTP 框架、库可以调整；以上是最终行为要求，并非要求自行解析 HTTP。起始代码尚未实现完整请求期限和有界退出，客户端期限是否完整也需要验证。
